apiVersion: v1
kind: Secret
metadata:
  name: servis-postgresql
  namespace: default
  labels:
    app: servis-postgresql
type: Opaque
data:
  postgresql-password: ODVkNDhmMzE1MmY2ZGI1ZWM0NzgzZmVkOTBiMTc3Zjc3NmNkMGVhOGI3MTViOTFkYTZmNDM2ODEyNTAy
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: servis-postgresql
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servis-postgresql
  template:
    metadata:
      labels:
        app: servis-postgresql
    spec:
      volumes:
        - name: data
          emptyDir: {}
      containers:
        - name: postgres
          image: postgres:13.5
          env:
            - name: POSTGRES_USER
              value: servis
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: servis-postgresql
                  key: postgresql-password
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: postgres
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1"
---
apiVersion: v1
kind: Service
metadata:
  name: servis-postgresql
  namespace: default
spec:
  selector:
    app: servis-postgresql
  ports:
    - port: 5432
